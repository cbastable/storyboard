<%= form_for(current_user.relationships.find_by_publisher_id(@user),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unsubscribe", class: "btn btn-large" %>
<% end %>