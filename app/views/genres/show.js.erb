<% if @primary_stories.all.count>0 -%>
$('.stories').append('<%= escape_javascript(render(partial: 'genre_stories')) %>');
$('a.hook').attr('href', '<%= genre_path(page: (params[:page].to_i || 1)+1, per_page: params[:per_page] || 8) %>').show();
<% else -%>
$('a.hook').hide();
<% end -%>