<%= form_for([@story, @comment]) do |f| %>
  <%= render 'shared/error_messages', object: @comment %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Click to comment..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>