<% if signed_in? %>
<%= form_for(@story.stats.build(story_id: @story.id, viewer_id: @user.id),
              remote: true) do |f| %>
  <%= f.hidden_field :story_id %>
  <%= f.hidden_field :viewer_id %>
  <%= f.submit "Pay with Storyboard Points", class: "btn btn-primary" %>
<% end %>


<%= form_for(@story.stats.build(story_id: @story.id, viewer_id: @user.id),
              remote: true) do |f| %>
  <%= f.hidden_field :story_id %>
  <%= f.hidden_field :viewer_id %>
  <%= f.submit "View story for free", class: "btn btn-primary" %>
<% end %>
<% end %>