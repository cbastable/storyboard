<%= form_for(@stat = @story.stats.find_by_viewer_id(current_user.id),
              url: no_vote_story_path(@stat),
              remote: true) do |f| %>
  <%= f.submit "Clear vote", class: "btn btn-small btn-primary" %>
<% end %>