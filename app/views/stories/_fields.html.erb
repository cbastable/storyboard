<%= render 'shared/error_messages', object: @story %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :genre %>
<%= f.text_field :genre %>

<div class="field">
<%= f.text_area :blurb, placeholder: "Blurb...(max. 150 characters)" %>
</div>

<div class="field">
<%= f.text_area :content, placeholder: "Write your story here...(max. 10,000 words)" %>
</div>