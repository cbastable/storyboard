<% provide(:title, 'Publish') %>
<h1>Publish story</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@story) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Update story", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>