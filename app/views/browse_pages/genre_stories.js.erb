<% for genre, primary_stories in @genre_stories %>

 <% if params[:param_name] == genre.name %>
 

 

   <% if primary_stories.all.count>0 %>




   $('#<%=genre.name%>').append('<%= escape_javascript(render partial: 'genre_stories',
                                locals: {genre: genre, primary_stories: primary_stories}) %>');           
   $('a.<%=genre.name%>.hook3').attr('href', '<%=
   
           case genre.name
                 when "Fiction" then
                          root_path(Fiction: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 when "Romance" then
                          root_path(Romance: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 when "Fantasy" then
                          root_path(Fantasy: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 when "Mystery" then
                          root_path(Mystery: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)                        
                 when "Science_fiction" then
                          root_path(Science_fiction: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 when "Horror" then
                          root_path(Horror: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 when "Crime" then
                          root_path(Crime: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 when "Paranormal" then
                          root_path(Paranormal: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 when "Urban_fantasy" then
                          root_path(Urban_fantasy: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 when "Thriller" then
                          root_path(Thriller: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 when "Teen" then
                          root_path(Teen: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 when "Western" then
                          root_path(Western: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 when "Fanfiction" then
                          root_path(Fanfiction: (params[:"#{genre.name}"].to_i || 1)+1,
                                         param_name: "#{genre.name}",
                                         per_page: params[:per_page] || 8)
                 else
                         #
                 
           end      
                                         
                                   %>').show();                                
                                   
    <% else %>                             
    $("a.<%=genre.name%>.hook3").hide();
    
    
    
    
    <% end %>

    


 
 <% end %>
 <% end %>