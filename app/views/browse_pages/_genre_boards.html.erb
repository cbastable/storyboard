<ul class="genres">
  <%= render 'genres' %>
</ul>
<%= link_to(root_path(page: (params[:page].to_i || 1)+1,
                                      per_page: params[:per_page] || 3),
                                      class: "hook btn btn-block btn-primary", remote: true) do %>
                                      <i class="icon-circle-arrow-down icon-white"></i> More...
                                    <% end %>