<ul class="genres">
  <%= render 'genres' %>
</ul>
<%= link_to(root_path(page: (params[:page].to_i || 2)+2,
                                      per_page: params[:per_page] || @genres_per_page),
                                      class: "hook btn btn-block btn-primary", remote: true) do %>
                                      <i class="icon-circle-arrow-down icon-white"></i> More...
                                    <% end %>